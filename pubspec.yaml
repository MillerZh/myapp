name: stock
description: A股纪律分析助手 — 基于买卖纪律规则的个股信号扫描
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  collection: ^1.19.1
  flutter_local_notifications: ^22.0.1
  workmanager: ^0.9.0+3
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
